.card-body
  .table-responsive
    %table#dataTable.table.table-bordered{cellspacing: '0', width: '100%'}
      %thead
        %tr
          %th.text-center #
          %th= t('records.stock.properties.name')
          %th= t('records.stock.properties.unit_price')
          %th= t('records.stock.properties.interest')
          %th= t('records.stock.properties.duration')
          %th.text-center= t('stocks.index.actions')
      %tbody
        - facade.stocks.each.with_index(facade.page_counter) do |item, index|
          = render partial: 'stocks/index/item', locals: { item: item, index: index }

    = render 'pagy/nav', pagy: facade.pagination